---
title: 'Example project'
description: 'A placeholder project entry — replace this with something you have built.'
link: 'https://example.com'
repo: 'https://github.com/AcroIsTrash/example'
tags: ['placeholder']
pubDate: 2026-05-01
---

This is an example project entry to show how the layout works. Each project is a
Markdown file in `src/content/projects/`.

## Frontmatter options

- `title`, `description` — required.
- `link` — a live/demo URL (optional).
- `repo` — a source code URL (optional).
- `featured: true` — surfaces the project first on the home page.
- `tags` — an array of labels.

Replace this file (or delete it) and add your real projects. The body supports
full Markdown, so you can include screenshots, code samples, and more.
